#include <stdio.h>
#include "main.h"

/**
* mul - Multiplication of two variables.
*@a: parameter the int will use to check for the argument of the function
*@b: parameter the int will use to check for the argument of the function
* Return: Always 0.
*/
int mul(int a, int b)
{
	return (a * b);
}

