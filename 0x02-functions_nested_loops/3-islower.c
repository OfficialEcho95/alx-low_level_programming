#include "main.h"
/**
 * isloweir - that checks for alphabetic characters 
 *
 * c - is the input char
 *
 * Return 0 (Success)
 */
int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
       	{
		return (0);
	}

}
